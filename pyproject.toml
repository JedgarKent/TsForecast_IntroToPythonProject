[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tsforecast"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pandas", "numpy", "statsmodels", "matplotlib"]

[tool.ruff]
line-length = 88